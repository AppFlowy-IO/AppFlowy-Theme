
import 'package:flutter/material.dart';

class LoadingCard extends StatelessWidget {
  LoadingCard({super.key, required this.width, required this.height});

  final double width;
  final double height;

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}